

<!-- <div class="row">
  <% @products.each do |product| %>
    <div class="col-xs-6 col-md-4">
      <%= link_to product_path(product) do %>
        <% if product.photos.present? %>
          <%= image_tag(product.photos[0].avatar.thumb.url, class: "thumbnail") %>
        <% else %>
          <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
        <% end %>
      <% end %>
      <%= product.title %>
      <% if product.discount.present? %>
        ￥ <%= product.price = product.price * product.discount/100 %>
      <% else %>
        ￥ <%= product.price %>
      <% end %>
    </div>
  <% end %>
</div> -->
<div class="col-md-10 col-centered">
  <!-- <div class="panel panel-default"> -->
  <div class="panel-body">
      <div class="row">
        <h3>此為預覽圖，連結在最下方<br>由於隊員生病，圖片部分要延遲兩天更新，抱歉<br></h3>
        <a href="https://goo.gl/78BMsw"><img src="http://i.imgur.com/0bYAZKj.png"></a><br>
        <% @products.each do |product| %>
          <div class="box col-xs-12 col-md-4 text-center">
          <%= link_to product_path(product) do %>
              <% if product.photos.present? %>
                  <%= image_tag(product.photos[0].avatar.medium.url) %>
              <% else %>
                  <%= image_tag("http://placehold.it/200x200&text=No Pic") %>
                  <!-- <%= image_tag(resume.user.aavatar.thumb.url, class: "img-responsive  user-img") %> -->
              <% end %>
           <% end %>
            <h6><%= product.title %></h6>
            <h4>￥ <%= product.price %></h4>
          </div>
        <% end %>
      </div>
      </div>
    <!-- </div> -->
    </div>
